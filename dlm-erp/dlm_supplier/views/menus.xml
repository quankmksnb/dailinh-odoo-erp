<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Additive: theo TDS 4.0, quyền NCC dùng chung cơ chế Partner đã có
         (không còn ir.rule thu hẹp riêng) — chỉ cần đảm bảo Kế toán nội bộ
         (vừa được cấp ACL ở trên) cũng thấy được menu NCC sẵn có của dl_sale. -->
    <record id="dl_sale.menu_dl_sale_supplier" model="ir.ui.menu">
        <field name="groups_id" eval="[(4, ref('dl_base.dl_group_accountant'))]"/>
    </record>

</odoo>
